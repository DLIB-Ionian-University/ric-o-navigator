{
  "name": "ric-navigator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-data.mjs",
    "dev": "npm run generate:data && vite",
    "build": "npm run generate:data && vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "v-network-graph": "^0.9.22",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
